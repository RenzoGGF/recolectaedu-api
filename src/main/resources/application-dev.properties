# ============================================
# DEVELOPMENT PROFILE - Desarrollo Local
# ============================================
# Usar con: mvn spring-boot:run -Dspring-boot.run.profiles=dev
# O en IDE: -Dspring.profiles.active=dev

# PostgreSQL database local
spring.datasource.url=jdbc:postgresql://localhost:5432/recolectaedu
spring.datasource.username=postgres
spring.datasource.password=adminadmin
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA configurations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server
server.port=8080

# JWT Configuration
jwt.secret=ClaveSecreta2444666668888888000000000
jwt.expiration=1800000
# 30 minutos en milisegundos

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.health.defaults.enabled=true